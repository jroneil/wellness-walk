const CACHE="wellness-window-shell-v27",SHELL=["/","/offline","/icons/icon-192.png","/icons/icon-512.png"];self.addEventListener("install",e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(SHELL))));self.addEventListener("message",e=>{if(e.data?.type==="SKIP_WAITING")self.skipWaiting()});self.addEventListener("activate",e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));self.addEventListener("fetch",e=>{const u=new URL(e.request.url);if(e.request.method!=="GET"||u.pathname.startsWith("/api/")||u.pathname.includes("oauth")||u.pathname.includes("/history/export/"))return;if(e.request.mode==="navigate"){e.respondWith(fetch(e.request).catch(()=>caches.match("/offline")));return}if(u.origin===location.origin&&(/\.(css|js|svg|png|ico)$/.test(u.pathname)||u.pathname.startsWith("/_next/static/")))e.respondWith(caches.match(e.request).then(v=>v||fetch(e.request).then(r=>{if(r.ok){const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy))}return r}))) });self.addEventListener("notificationclick",e=>{e.notification.close();const action=e.action==="start"?"start":e.action==="dismiss"?"dismiss":null,id=typeof e.notification.data?.recommendationId==="string"?e.notification.data.recommendationId:"";const url=action&&/^[a-zA-Z0-9_-]{1,120}$/.test(id)?`/?notificationAction=${action}&recommendationId=${encodeURIComponent(id)}`:"/";e.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{const existing=list.find(c=>"focus"in c);if(existing){existing.navigate(url);return existing.focus()}return clients.openWindow(url)}))});
