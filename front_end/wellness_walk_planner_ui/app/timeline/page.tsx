"use client";
import Link from "next/link";import { useEffect,useState } from "react";import type { WeatherResponse } from "../types";import { loadCalendarEvents } from "../calendar/calendar-service";import { OpportunityTimeline } from "./opportunity-timeline";
export default function TimelinePage(){const [weather,setWeather]=useState<WeatherResponse|null>(null);useEffect(()=>{try{const raw=sessionStorage.getItem("wellness-window-last-weather:v1");if(raw)setWeather(JSON.parse(raw));}catch{}},[]);return <main className="min-h-screen bg-slate-50 px-4 py-10 text-slate-900"><div className="mx-auto max-w-6xl"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold uppercase tracking-[.2em] text-emerald-700">Wellness Window</p><h1 className="mt-2 text-3xl font-semibold">Recommendation Timeline</h1></div><Link href="/" className="rounded-2xl border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">← Dashboard</Link></div>{weather?<OpportunityTimeline weather={weather} events={loadCalendarEvents()}/>:<p className="mt-8 rounded-3xl border border-dashed border-slate-300 bg-white p-8 text-slate-600">Load a recommendation on the dashboard to create today&apos;s opportunity timeline.</p>}</div></main>}
