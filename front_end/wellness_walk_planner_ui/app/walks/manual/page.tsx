"use client";import Link from"next/link";import{FormEvent,useState}from"react";import type{PerceivedQuality}from"../../types";import{addManualWalk}from"../../wellness-actions";export default function ManualWalkPage(){const[start,setStart]=useState(""),[duration,setDuration]=useState(20),[quality,setQuality]=useState<PerceivedQuality|null>(null),[notes,setNotes]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();try{await addManualWalk(new Date(start).toISOString(),duration,quality,notes);setMessage(`You recorded a ${duration}-minute walk.`);setNotes("")}catch(error){setMessage(error instanceof Error?error.message:"Walk could not be saved.")}}return <main className="min-h-screen bg-slate-50 px-4 py-10"><form onSubmit={submit} className="mx-auto max-w-xl rounded-3xl border bg-white p-6"><div className="flex justify-between"><h1 className="text-2xl font-semibold">Record a manual walk</h1><Link href="/history">← History</Link></div><p className="mt-2 text-sm text-slate-600">Use this for a completed walk that was not tied to a recommendation.</p><label className="mt-5 block font-semibold">Start date and time<input required type="datetime-local" value={start} onChange={e=>setStart(e.target.value)} className="mt-1 w-full rounded-xl border p-2"/></label><label className="mt-4 block font-semibold">Duration in minutes<input required type="number" min="1" max="480" value={duration} onChange={e=>setDuration(Number(e.target.value))} className="mt-1 w-full rounded-xl border p-2"/></label><label className="mt-4 block font-semibold">How did it feel? (optional)<select value={quality??""} onChange={e=>setQuality((e.target.value||null)as PerceivedQuality|null)} className="mt-1 w-full rounded-xl border p-2"><option value="">Not specified</option>{["RESTORATIVE","GOOD","NEUTRAL","UNCOMFORTABLE"].map(v=><option key={v}>{v}</option>)}</select></label><label className="mt-4 block font-semibold">Notes (optional)<textarea maxLength={1000} value={notes} onChange={e=>setNotes(e.target.value)} className="mt-1 w-full rounded-xl border p-2"/></label><button className="mt-5 rounded-xl bg-emerald-700 px-4 py-2 font-semibold text-white">Save walk</button>{message?<p role="status" className="mt-3">{message}</p>:null}</form></main>}
