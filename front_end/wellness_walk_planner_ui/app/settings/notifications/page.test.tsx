import { fireEvent,render,screen } from "@testing-library/react";import { beforeEach,describe,expect,it,vi } from "vitest";import NotificationSettingsPage from "./page";import { settingsStorageKey } from "../settings";
describe("NotificationSettingsPage",()=>{beforeEach(()=>{localStorage.clear();Object.defineProperty(window,"Notification",{value:undefined,configurable:true});});it("shows unsupported state and saves notification preferences",async()=>{render(<NotificationSettingsPage/>);expect(await screen.findByText(/Browser permission: UNSUPPORTED/)).toBeInTheDocument();expect(screen.getByText(/does not support notifications/)).toBeInTheDocument();fireEvent.click(screen.getByRole("checkbox",{name:"Enable notifications"}));fireEvent.change(screen.getByLabelText("Minimum notification score"),{target:{value:"85"}});fireEvent.click(screen.getByRole("button",{name:"Save notification settings"}));expect(await screen.findByText("Notification settings saved.")).toBeInTheDocument();expect(localStorage.getItem(settingsStorageKey)).toContain('"minimumNotificationScore":85');});});
